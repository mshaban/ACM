#include <stdio.h>
#include <algorithm>
using namespace std;

int n, k, arr[15], t, inv, cnt;

int main(int argc, char const *argv[])
{
    scanf("%d", &t);
    while (t--)
    {
        
    }
    return 0;
}
